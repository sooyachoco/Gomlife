name: Legacy layout guard

on:
  workflow_run:
    workflows: ["Sync Gomlife from gomlife2"]
    types: [completed]

permissions:
  contents: read

jobs:
  guard:
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-latest
    steps:
      - name: No-op
        run: echo "Layout is maintained by fix-susi-banner.yml"
